import XCTest
@testable import AccessibiltySupport

final class AccessibiltySupportTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AccessibiltySupport().text, "Hello, World!")
    }
}
